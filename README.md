# HDB Participant v6.9.3

GitHub Pages ready: upload `index.html` to the repository root.

Fixed:
- white screen caused by Participant CSS hiding `#about`
- project presentation is the first screen
- platform is hidden until requested
- email/password authentication opens when entering the platform
- Participant and Researcher Supabase auth sessions use different storage keys
- confirmation redirect requests the current Participant Pages URL
- HDB scientific baseline algorithm unchanged
