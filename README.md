# HDB Participant v6.9.2
GitHub Pages ready. `index.html` is the entry point.

Flow: project presentation → email/password authentication → automatic pseudonymous HDB code → participant platform → Supabase sync.
